#include <stdio.h>

int main(void) {
	int a[5],i,count=0;
	for(i=0;i<5;i++)
	scanf("%d",&a[i]);
	for(i=0;i<5;i++)
	{
	    if(a[i]>=10)
	    count++;
	}
	if(count!=0)
	printf("%d",count);
	return 0;
}
