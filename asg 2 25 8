#include<stdio.h>
void main()
{
char str[50];
int i,count=1;
printf("enter string\n");
gets(str);
for(i=0;str[i]!='\0';i++)
{
if(str[i]==' ')
count++;
}
printf("%d",count);
}

output---
enter string
this is coding assignment
4
