#include<stdio.h>
void display(int start,int end)
{
if(start>end)
return;
else
printf("%d  ",start);
display(start+1,end);
}  
void main()
{
int n,m;
scanf("%d%d",&n,&m);
display(n,m);
}

output---
1
5
1  2  3  4  5
