#include<stdio.h>
void main()
{
char str[20];
int i;
printf("enter string\n");
gets(str);
for(i=0;str[i]!='\0';i++)
{
if(str[i]%2==0)
str[i]=str[i]-32;
}
puts(str);
}

output---
enter string
aditya
aDiTya
